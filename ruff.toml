line-length = 120

[lint]
select = ["E4", "E7", "E9", "F", "I", "B", "C4", "SIM", "RUF"]

# FastAPI's dependency system is built on calls in default arguments:
# `db: Session = Depends(get_db)` is the documented pattern, not the shared-mutable-default
# bug B008 exists to catch. It fires 113 times here and every one of them is correct.
ignore = ["B008"]
import { Text, View } from "react-native";

export default function Verify() {
  return (
    <View className="flex-1 items-center justify-center bg-ink">
      <Text className="text-lg text-fg">Verify Email</Text>
    </View>
  );
}